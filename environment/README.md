# Environment Form CLI

Generate `非嵌入式软件环境` from a JSON input file.

## Quick Start

```bash
python3 src/main.py init --path input_environment.json
python3 src/main.py run --input-json input_environment.json
```
