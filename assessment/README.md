# Assessment Form CLI

Generate `产品评估申请` Excel from input JSON.

## Quick Start

```bash
python3 src/main.py init --path input_assessment.json
python3 src/main.py run --input-json input_assessment.json
```
